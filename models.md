🛑 GIT RESTRICTIONS - READ CAREFULLY

    NEVER execute git commit or git push commands.

    NEVER use the -m flag to stage or commit changes automatically.

    You are STRICTLY PROHIBITED from managing the version control system.

    Your only responsibility is to write code and suggest changes. I (the human) will handle all Git operations.